# 企业级 RAG 知识库系统 —— 部署启动教程

> 本教程适用于 **Windows / macOS / Linux** 全平台，按顺序照做即可从零跑通系统。
> 命令分别给出 PowerShell（Windows）与 Bash（macOS/Linux）两种写法，请按自己的系统选择。

---

## 目录

1. [环境要求](#1-环境要求)
2. [安装基础软件](#2-安装基础软件)
3. [获取项目代码](#3-获取项目代码)
4. [初始化 MySQL 数据库](#4-初始化-mysql-数据库)
5. [启动中间件（Redis / Elasticsearch）](#5-启动中间件redis--elasticsearch)
6. [配置并启动后端](#6-配置并启动后端)
7. [启动前端](#7-启动前端)
8. [验证与体验](#8-验证与体验)
9. [（可选）接入大模型](#9-可选接入大模型)
10. [常见问题排查（FAQ）](#10-常见问题排查faq)
11. [（可选）生产环境部署建议](#11-可选生产环境部署建议)

---

## 1. 环境要求

| 软件 | 版本要求 | 用途 | 是否必须 |
| --- | --- | --- | --- |
| Python | 3.10 及以上 | 后端运行时 | ✅ 必须 |
| Node.js | 20 及以上 | 前端构建与开发服务器 | ✅ 必须 |
| MySQL | 8.x | 业务数据库 | ✅ 必须 |
| Elasticsearch | 8.x | 全文 + 向量检索引擎 | ✅ 必须（缺失时问答检索不可用，但应用可启动） |
| Redis | 5.0+ 任意稳定版 | 语义缓存 / 仪表盘指标 | ⭕ 推荐（缺失时自动跳过缓存，不影响主链路） |
| DeepSeek API Key | — | 大模型问答 | ⭕ 可选（不配置自动降级为检索摘要式回答） |

> 💡 本系统**不需要下载任何 AI 模型权重**，向量化采用内置本地哈希嵌入，离线环境也能完整运行。

各端口默认占用（如被占用需修改配置）：

| 服务 | 默认端口 |
| --- | --- |
| MySQL | 3306 |
| Redis | 6379 |
| Elasticsearch | 9200 |
| 后端 FastAPI | 8000 |
| 前端 Vite | 5173 |

---

## 2. 安装基础软件

### 2.1 Python 3.10+

- **Windows**：到 [python.org/downloads](https://www.python.org/downloads/) 下载安装包，安装时务必勾选 **"Add Python to PATH"**
- **macOS**：`brew install python@3.12`
- **Linux (Ubuntu/Debian)**：`sudo apt install python3 python3-venv python3-pip`

验证：

```bash
python --version    # Windows
python3 --version   # macOS / Linux
```

### 2.2 Node.js 20+

- **Windows / macOS**：到 [nodejs.org](https://nodejs.org/) 下载 LTS 安装包
- **Linux**：推荐用 [nvm](https://github.com/nvm-sh/nvm) 安装：`nvm install 20`

验证：

```bash
node -v    # 应输出 v20.x 或更高
npm -v
```

### 2.3 MySQL 8.x

- **Windows**：[MySQL Installer](https://dev.mysql.com/downloads/installer/) 图形化安装，记住 root 密码
- **macOS**：`brew install mysql && brew services start mysql`
- **Linux**：`sudo apt install mysql-server && sudo systemctl start mysql`

验证：

```bash
mysql --version
```

### 2.4 Redis

- **Windows**：官方不提供 Windows 版，两种方式任选：
  - 方式 A：下载社区维护的 [Redis for Windows](https://github.com/tporadowski/redis/releases)（解压后运行 `redis-server.exe`）
  - 方式 B：使用 Docker（见 2.6）
- **macOS**：`brew install redis && brew services start redis`
- **Linux**：`sudo apt install redis-server && sudo systemctl start redis`

验证：

```bash
redis-cli ping    # 返回 PONG 即正常
```

### 2.5 Elasticsearch 8.x

到 [elastic.co/downloads/elasticsearch](https://www.elastic.co/downloads/elasticsearch) 下载对应平台的压缩包并解压（无需安装）。

**⚠️ 重要：为简化本地部署，建议关闭 ES 的安全认证。** 编辑解压目录下 `config/elasticsearch.yml`，在文件末尾添加：

```yaml
xpack.security.enabled: false
xpack.security.http.ssl.enabled: false
```

启动：

```powershell
# Windows（PowerShell，进入解压目录）
.\bin\elasticsearch.bat
```

```bash
# macOS / Linux（进入解压目录）
./bin/elasticsearch
```

验证（另开一个终端）：

```bash
curl http://127.0.0.1:9200
# 返回包含 "cluster_name" 的 JSON 即正常
```

> 💡 若保留安全认证，请记下 elastic 用户密码，稍后填入后端 `.env` 的 `ELASTICSEARCH_USERNAME / ELASTICSEARCH_PASSWORD`，并将 `ELASTICSEARCH_URL` 改为 `https://...`、`ES_VERIFY_CERTS=false`。

### 2.6 （替代方案）用 Docker 一键启动三件套

如果本机装有 Docker，可跳过 2.3～2.5，直接：

```bash
docker run -d --name rag-mysql -p 3306:3306 -e MYSQL_ROOT_PASSWORD=root mysql:8 --character-set-server=utf8mb4
docker run -d --name rag-redis -p 6379:6379 redis:7
docker run -d --name rag-es -p 9200:9200 -e discovery.type=single-node -e xpack.security.enabled=false -e ES_JAVA_OPTS="-Xms512m -Xmx512m" docker.elastic.co/elasticsearch/elasticsearch:8.17.0
```

---

## 3. 获取项目代码

将项目文件夹放到任意路径（**建议路径不含中文与空格**，可避免部分工具链的兼容问题；本教程以下用 `<项目根目录>` 指代）。

项目结构确认：

```
<项目根目录>/
├── backend/          # 后端
│   ├── app/
│   ├── sql/          # schema.sql（建表）+ seed.sql（种子数据）
│   ├── .env.example  # 配置模板
│   └── requirements.txt
├── frontend/         # 前端
└── 示例文档/          # 34 份测试文档，可上传体验
```

---

## 4. 初始化 MySQL 数据库

> 所有平台命令相同。`-p` 后回车会提示输入 root 密码。

```bash
# 1) 创建数据库（utf8mb4 编码，完整支持中文）
mysql -u root -p --default-character-set=utf8mb4 -e "CREATE DATABASE IF NOT EXISTS rag_kb DEFAULT CHARACTER SET utf8mb4"

# 2) 建表（在 <项目根目录> 下执行）
mysql -u root -p --default-character-set=utf8mb4 rag_kb < backend/sql/rag_kb.sql
```

> Windows PowerShell 下若 `<` 重定向报错，改用：
>
> ```powershell
> Get-Content backend/sql/rag_kb.sql -Raw | mysql -u root -p --default-character-set=utf8mb4 rag_kb
> ```

验证：

```bash
mysql -u root -p --default-character-set=utf8mb4 rag_kb -e "SHOW TABLES;"
# 应看到 14 张表：user / role / knowledge_base / document / chunk / conversation ...
```

---

## 5. 启动中间件（Redis / Elasticsearch）

> ⚠️ **启动顺序非常重要：Elasticsearch 必须先于后端启动**，否则后端启动时索引初始化会跳过（后续虽可自动重试，但首次体验建议按序启动）。

推荐顺序：

```
① MySQL（通常随系统服务已在运行）
② Redis
③ Elasticsearch（等待约 30 秒完全就绪）
④ 后端
⑤ 前端
```

确认三个中间件都已就绪：

```bash
redis-cli ping                # PONG
curl http://127.0.0.1:9200    # 返回集群信息 JSON
mysql -u root -p --default-character-set=utf8mb4 -e "SELECT 1"
```

---

## 6. 配置并启动后端

### 6.1 创建配置文件

```powershell
# Windows PowerShell
cd <项目根目录>\backend
Copy-Item .env.example .env
```

```bash
# macOS / Linux
cd <项目根目录>/backend
cp .env.example .env
```

用任意编辑器打开 `.env`，**按本机实际情况修改以下几项**（其余保持默认即可）：

```ini
# MySQL：把 root:root 改成你的账号:密码
DATABASE_URL=mysql+pymysql://root:你的密码@127.0.0.1:3306/rag_kb?charset=utf8mb4

# Elasticsearch：若按 2.5 关闭了安全认证，以下保持默认即可
ELASTICSEARCH_URL=http://127.0.0.1:9200
ELASTICSEARCH_USERNAME=elastic
ELASTICSEARCH_PASSWORD=changeme

# Redis：本机默认无需修改
REDIS_URL=redis://127.0.0.1:6379/0

# 大模型：可留空（自动降级），配置方法见第 9 节
LLM_API_KEY=
```

### 6.2 创建虚拟环境并安装依赖

```powershell
# Windows PowerShell（在 backend 目录下）
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

> 若 PowerShell 提示"禁止运行脚本"，先执行：
> `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned`，然后重新激活虚拟环境。

```bash
# macOS / Linux（在 backend 目录下）
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

> 国内网络较慢时可加清华镜像：
> `pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`

### 6.3 启动后端

```bash
# 两平台命令相同（确保虚拟环境已激活、当前在 backend 目录）
python -m uvicorn app.main:app --reload --port 8000
```

看到类似输出即启动成功：

```
INFO:  Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
INFO:  Application startup complete.
```

健康检查（另开终端或浏览器访问）：

```
http://127.0.0.1:8000/api/health
```

返回 JSON 中会标明 `es / redis / llm` 三个依赖的可用状态，全部为 true 表示环境完备。

> 接口文档（FastAPI 自动生成）：`http://127.0.0.1:8000/docs`

---

## 7. 启动前端

**另开一个新终端**（保持后端终端运行）：

```powershell
# Windows PowerShell
cd <项目根目录>\frontend
npm install
npm run dev
```

```bash
# macOS / Linux
cd <项目根目录>/frontend
npm install
npm run dev
```

> npm install 较慢时可换国内镜像：`npm install --registry=https://registry.npmmirror.com`

看到以下输出即成功：

```
VITE v5.x  ready in xxx ms
➜  Local:   http://127.0.0.1:5173/
```

浏览器访问 **http://127.0.0.1:5173** 即进入系统。

---

## 8. 验证与体验

### 8.1 默认账号

种子数据内置以下演示账号（登录后建议及时修改密码）：

| 账号 | 密码 | 角色 |
| --- | --- | --- |
| admin | admin123 | 系统管理员（全部功能） |
| zhangsan | 123456 | 知识库管理员 |
| lisi | 123456 | 普通用户 |
| wangwu | 123456 | 知识库管理员 |

### 8.2 完整链路自测（约 3 分钟）

1. 用 `admin / admin123` 登录，进入**仪表盘**确认页面正常；
2. 进入**文档管理**，选择知识库"考勤与假期制度"，上传 `示例文档/01-考勤与假期制度/考勤管理制度.md`；
3. 观察文档状态从 `indexing` 变为 `ready`（可在**任务中心**查看索引进度）；
4. 进入**智能问答**，提问："迟到多久算旷工？"
5. 确认：页面依次出现"路由中 → 检索中 → 生成中"，流式输出回答，并附引用来源；
6. 进入**会话历史**，确认刚才的对话已按账号保存。

全部通过即部署成功 ✅。

---

## 9. （可选）接入大模型

不配置大模型系统也能运行（自动降级为"检索片段摘要式回答"）；配置后可获得完整智能链路（自动选库、问题分解、幻觉校验、自然语言回答）。

1. 到 [DeepSeek 开放平台](https://platform.deepseek.com/) 注册并创建 API Key；
2. 编辑 `backend/.env`：

   ```ini
   LLM_API_KEY=sk-xxxxxxxxxxxxxxxx
   LLM_BASE_URL=https://api.deepseek.com
   LLM_MODEL=deepseek-v4-pro
   LLM_ENABLED=true
   ```

3. 重启后端，访问 `/api/health` 确认 `llm: true`。

> 任何 OpenAI 兼容协议的服务（如 Ollama 本地模型、通义千问兼容端点等）均可接入，改 `LLM_BASE_URL` 与 `LLM_MODEL` 即可。

---

## 10. 常见问题排查（FAQ）

### Q1：后端启动日志提示 ES 索引初始化失败 / 问答检索不到内容

**原因**：Elasticsearch 未启动，或启动晚于后端。
**解决**：先启动 ES 并等待其完全就绪（`curl http://127.0.0.1:9200` 有返回），再启动后端。若后端已在运行，重启后端或上传一篇文档触发索引自动重建即可。

### Q2：`ModuleNotFoundError: No module named 'pypdf'`（或其他包）

**原因**：依赖装到了系统 Python，而运行用的是虚拟环境（或相反）——两个环境不一致。
**解决**：确认终端提示符带有 `(.venv)` 前缀后再执行 `pip install -r requirements.txt` 与启动命令；或用 `pip -V` 检查 pip 属于哪个环境。

### Q3：Windows 下虚拟环境中 Python 报 `ImportError: DLL load failed while importing _socket`

**原因**：Windows 注册表残留了指向其他 Python 安装目录的 `PYTHONHOME/PYTHONPATH`，或注册表 Python 路径与实际不符，导致 venv 加载了错误的 DLL。
**解决**：检查系统环境变量中是否存在 `PYTHONHOME`、`PYTHONPATH`，有则删除后重开终端；仍不行则删除 `.venv` 目录，用完整路径的目标 Python 重新创建（如 `D:\Python312\python.exe -m venv .venv`）。

### Q4：MySQL 中文乱码 / Emoji 写入失败

**原因**：连接或库未使用 utf8mb4。
**解决**：确认 `DATABASE_URL` 末尾带 `?charset=utf8mb4`；命令行操作数据库时始终携带 `--default-character-set=utf8mb4`；确认建库语句包含 `DEFAULT CHARACTER SET utf8mb4`。

### Q5：前端页面打开但所有请求报错 / 跨域错误

**排查顺序**：
1. 后端是否在 8000 端口运行（访问 `http://127.0.0.1:8000/api/health`）；
2. 是否用 `http://127.0.0.1:5173` 或 `http://localhost:5173` 访问前端——其他地址（如局域网 IP）需追加到 `.env` 的 `CORS_ORIGINS`；
3. 浏览器控制台若见 401，退出重新登录即可（令牌过期）。

### Q6：端口被占用（8000 / 5173 / 9200 等）

```powershell
# Windows：查看占用进程
netstat -ano | findstr :8000
```

```bash
# macOS / Linux
lsof -i :8000
```

结束占用进程，或换端口启动：后端 `--port 8001`（同时修改前端 `vite.config.ts` 的代理目标），前端 `npm run dev -- --port 5174`（同时把新地址加入 `CORS_ORIGINS`）。

### Q7：Elasticsearch 启动即退出 / 内存不足

**解决**：编辑 ES 目录 `config/jvm.options`，添加两行限制堆内存：

```
-Xms512m
-Xmx512m
```

Linux 下若报 `max virtual memory areas vm.max_map_count [65530] is too low`：

```bash
sudo sysctl -w vm.max_map_count=262144
```

### Q8：上传文档后状态一直 indexing 或变为 failed

**排查**：进入**任务中心**查看任务的错误信息；常见原因为 ES 不可用（参见 Q1）或文件本身无法解析。修复环境后在任务中心点击**重试**，或在文档列表使用**重建索引**。

### Q9：`pip install` 或 `npm install` 特别慢 / 超时

使用国内镜像：

```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
npm install --registry=https://registry.npmmirror.com
```

---

## 11. （可选）生产环境部署建议

开发模式（`--reload` + Vite dev server）仅适合本地使用。对外提供服务建议：

### 11.1 前端构建 + Nginx 托管

```bash
cd frontend
npm run build        # 产物输出到 frontend/dist
```

Nginx 配置示例：

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端静态资源
    location / {
        root /path/to/frontend/dist;
        try_files $uri $uri/ /index.html;   # 支持前端路由刷新
    }

    # 后端 API 反向代理
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        # SSE 流式必需：关闭缓冲
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 300s;
    }
}
```

> ⚠️ `/api/` 位置块必须关闭 `proxy_buffering`，否则 SSE 流式问答会被 Nginx 缓冲成"一次性输出"。

### 11.2 后端进程管理

```bash
# 多 worker 启动（去掉 --reload）
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 2
```

- Linux：用 systemd 编写服务单元实现开机自启与崩溃拉起；
- Windows：可用 NSSM 将其注册为 Windows 服务。

### 11.3 安全加固清单

- [ ] `.env` 中 `APP_SECRET_KEY` 更换为随机长字符串
- [ ] 修改全部默认账号密码（admin/admin123 等）
- [ ] `CORS_ORIGINS` 收紧为实际前端域名
- [ ] MySQL / Redis / ES 不对公网暴露端口，仅允许后端主机访问
- [ ] ES 生产环境开启认证与 TLS，并同步更新 `.env` 中的 ES 配置
- [ ] 定期备份 MySQL（ES 索引可随时从 MySQL chunk 表重建，无需单独备份）

---

## 附：一览式启动速查卡

环境装好后，日常启动只需 4 步（顺序执行）：

| 步骤 | Windows PowerShell | macOS / Linux |
| --- | --- | --- |
| 1. 启动 Redis | `redis-server.exe` | `brew services start redis` / `systemctl start redis` |
| 2. 启动 ES（等 30 秒） | `ES目录\bin\elasticsearch.bat` | `ES目录/bin/elasticsearch` |
| 3. 启动后端 | `cd backend; .\.venv\Scripts\Activate.ps1; python -m uvicorn app.main:app --reload --port 8000` | `cd backend && source .venv/bin/activate && python -m uvicorn app.main:app --reload --port 8000` |
| 4. 启动前端 | `cd frontend; npm run dev` | `cd frontend && npm run dev` |

访问 `http://127.0.0.1:5173`，账号 `admin / admin123`。
